<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Todo List: </h1>
    <ul class="todoItems">
    <!-- loop through todo items -->
    <% for(let i=0; i < items.length; i++) {%>
        <li class="item">
            <% if(items[i].completed === true) {%> 
                <span class='completed'><%= items[i].thing %></span> <!-- add completed class if item is completed-->
            <% }else{ %>
                <span><%= items[i].thing %></span> <!-- else, no class -->
            <% } %>    
            <span class='fa fa-trash'></span> <!-- delete button, styled as a trashcan -->
        </li>
    <% } %>
    </ul>

    <h2>Left to do: <%= left %></h2> <!-- here is where the count of incomplete tasks will be printed -->

    <h2>Add A Todo:</h2>

    <form action="/addTodo" method="POST"> <!-- submission form for new todo-->
        <input type="text" placeholder="Thing To Do" name="todoItem">
        <input type="submit">
    </form>
    

    <script src='js/main.js'></script>
</body>
</html>
